from django.urls import path
# from django.conf.urls import url

from apps.emergency import views

urlpatterns = [

    # Websocket 测试API
    # path("", views.IndexView.as_view()),
    # url('kafka', views.kafka),
    # url('kafka_seek', views.kafka_seek),
    # url('omnibus', views.omnibus),
]
